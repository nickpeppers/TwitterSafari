<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
  xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:local="clr-namespace:TwitterSafari"
  x:Class="TwitterSafari.MainPage">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="3*"/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    
    <Entry x:Name="_searchEntry" Placeholder="Search"/>
    <Button Grid.Column="1" Text="GO" Clicked="OnSearchClicked"/>

    <ListView Grid.ColumnSpan="2" Grid.Row="1" ItemsSource="{Binding Tweets}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ImageCell ImageSource="{Binding ImageUrl}" Text="{Binding ScreenName}" Detail="{Binding Text}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>

</ContentPage>